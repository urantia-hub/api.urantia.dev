import { createApp } from "../lib/app.ts";

// Auth infrastructure routes — OAuth endpoints for third-party apps.
// Implemented in Phase 2/3 of the unified auth layer.
export const authRoute = createApp();
